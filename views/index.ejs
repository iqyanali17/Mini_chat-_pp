<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Min WhatsApp</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-gradient-to-br from-green-50 to-teal-50 min-h-screen">
    <div class="container mx-auto px-4 py-8 max-w-4xl">
        <!-- Header -->
        <div class="bg-gradient-to-r from-green-500 to-teal-500 rounded-2xl shadow-xl p-6 mb-8">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-3">
                    <a href="/" class="text-white hover:text-green-100 transition-colors" title="Home">
                        <i class="fas fa-home text-xl"></i>
                    </a>
                    <div class="bg-white rounded-full p-3">
                        <i class="fab fa-whatsapp text-green-500 text-2xl"></i>
                    </div>
                    <div>
                        <h1 class="text-3xl font-bold text-white">All Chats</h1>
                        <p class="text-green-100 text-sm"><%= chats.length %> message<%= chats.length !== 1 ? 's' : '' %></p>
                    </div>
                </div>
                <form method="GET" action="/chats/new">
                    <button type="submit" class="bg-white text-green-600 hover:bg-green-50 px-6 py-3 rounded-xl font-semibold shadow-lg transition-all duration-200 transform hover:scale-105 flex items-center space-x-2">
                        <i class="fas fa-plus"></i>
                        <span>New Chat</span>
                    </button>
                </form>
            </div>
        </div>

        <!-- Chats List -->
        <% if(chats.length === 0) { %>
            <div class="bg-white rounded-2xl shadow-lg p-12 text-center">
                <i class="fas fa-comments text-gray-300 text-6xl mb-4"></i>
                <h3 class="text-2xl font-semibold text-gray-600 mb-2">No chats yet</h3>
                <p class="text-gray-400 mb-6">Start a conversation by creating a new chat!</p>
                <form method="GET" action="/chats/new">
                    <button type="submit" class="bg-gradient-to-r from-green-500 to-teal-500 text-white px-8 py-3 rounded-xl font-semibold shadow-lg hover:shadow-xl transition-all duration-200 transform hover:scale-105">
                        Create Your First Chat
                    </button>
                </form>
            </div>
        <% } else { %>
            <div class="space-y-4">
                <% for(let chat of chats) { %>
                    <div class="bg-white rounded-2xl shadow-lg hover:shadow-xl transition-all duration-200 overflow-hidden">
                        <div class="p-6">
                            <!-- Chat Header -->
                            <div class="flex items-start justify-between mb-4">
                                <div class="flex-1">
                                    <div class="flex items-center space-x-3 mb-2">
                                        <div class="bg-gradient-to-r from-green-400 to-teal-400 rounded-full w-12 h-12 flex items-center justify-center text-white font-bold text-lg">
                                            <%= chat.from.charAt(0).toUpperCase() %>
                                        </div>
                                        <div>
                                            <p class="font-semibold text-gray-800">
                                                <i class="fas fa-user text-green-500 mr-2"></i>
                                                <%= chat.from %>
                                            </p>
                                            <p class="text-sm text-gray-500">
                                                <i class="fas fa-paper-plane text-teal-500 mr-1"></i>
                                                To: <span class="font-medium"><%= chat.to %></span>
                                            </p>
                                        </div>
                                    </div>
                                </div>
                                <div class="text-right">
                                    <p class="text-xs text-gray-400 mb-1">
                                        <i class="far fa-clock mr-1"></i>
                                        <%= chat.created_at.toString().split(" ")[4] %>
                                    </p>
                                    <p class="text-xs text-gray-400">
                                        <%= chat.created_at.toString().split(" ").slice(0,4).join(" ") %>
                                    </p>
                                </div>
                            </div>

                            <!-- Message -->
                            <div class="bg-gradient-to-r from-green-100 to-teal-100 rounded-xl p-4 mb-4 border-l-4 border-green-500">
                                <p class="text-gray-800 leading-relaxed"><%= chat.msg %></p>
                            </div>

                            <!-- Actions -->
                            <div class="flex space-x-3 pt-4 border-t border-gray-100">
                                <form method="GET" action="/chats/<%=chat._id%>/edit" class="flex-1">
                                    <button type="submit" class="w-full bg-blue-500 hover:bg-blue-600 text-white px-4 py-2 rounded-lg font-medium transition-all duration-200 transform hover:scale-105 flex items-center justify-center space-x-2">
                                        <i class="fas fa-edit"></i>
                                        <span>Edit</span>
                                    </button>
                                </form>
                                <form method="POST" action="/chats/<%=chat._id%>?_method=DELETE" class="flex-1">
                                    <button type="submit" onclick="return confirm('Are you sure you want to delete this chat?')" class="w-full bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-lg font-medium transition-all duration-200 transform hover:scale-105 flex items-center justify-center space-x-2">
                                        <i class="fas fa-trash"></i>
                                        <span>Delete</span>
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% } %>
            </div>
        <% } %>
    </div>
</body>
</html>
 